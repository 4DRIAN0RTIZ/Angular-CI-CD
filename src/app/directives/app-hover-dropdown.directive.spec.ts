import { AppHoverDropdownDirective } from './app-hover-dropdown.directive';

describe('AppHoverDropdownDirective', () => {
  it('should create an instance', () => {
    const directive = new AppHoverDropdownDirective();
    expect(directive).toBeTruthy();
  });
});
